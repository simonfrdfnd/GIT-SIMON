<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="MapAlarm.AlarmFrame">
  <ContentView.Content>
    <Frame BackgroundColor="#7e7e7e" Padding="24" CornerRadius="20" Margin="10">
      <Frame.GestureRecognizers>
        <TapGestureRecognizer Tapped="OnTapped"/>
      </Frame.GestureRecognizers>
      <Grid x:Name="layout">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="1*"/>
          <ColumnDefinition Width="3*"/>
          <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <CheckBox Grid.Column="0" x:Name="cb"/>
        <Label Text="New Alarm" HorizontalTextAlignment="Center" TextColor="White" FontSize="20" Grid.Column="1" x:Name="AlarmName"/>
        <Switch x:Name="styleSwitch" Grid.Column="2" OnColor="Coral" Toggled="OnToggle"/>
      </Grid>
    </Frame>
  </ContentView.Content>
</ContentView>