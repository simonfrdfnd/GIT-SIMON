<UserControl x:Class="NexioMax3.Definition.View.TerminalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NexioMax3.Definition.View"
             mc:Ignorable="d">
  <Grid>
     <TextBox Name="TerminalTextBox"
             Background="Black"
             Foreground="White"
             FontFamily="Consolas"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             TextWrapping="Wrap"
             AcceptsReturn="True"
             PreviewKeyDown="TerminalTextBox_PreviewKeyDown"
             SelectionChanged="TerminalTextBox_SelectionChanged"
             IsEnabled="{Binding EnableTerminal}"
             Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}">

      <TextBox.InputBindings>
        <KeyBinding Command="{Binding OnEnterCommand}" Key="Enter"/>
      </TextBox.InputBindings>
    </TextBox>
  </Grid>
</UserControl>
