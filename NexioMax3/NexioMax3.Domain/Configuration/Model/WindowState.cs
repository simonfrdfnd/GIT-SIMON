namespace NexioMax3.Domain.Configuration.Model
{
  /// <summary>
  /// Proxy pour ne pas ramener tout presentaion framework dans la DLL de domain
  /// </summary>
  public enum WindowState
  {
    Normal,
    Maximized,
    Minimized,
  }
}